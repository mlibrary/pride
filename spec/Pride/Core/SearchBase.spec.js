const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Core.SearchBase', function () {
  it('works', function () {
    expect(Pride.Core.SearchBase).to.not.be.null;
  });
});
