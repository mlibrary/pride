const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Core.Record', function () {
  it('works', function () {
    expect(Pride.Core.Record).to.not.be.null;
  });
});
