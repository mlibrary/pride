const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Core.Datastore', function () {
  it('works', function () {
    expect(Pride.Core.Datastore).to.not.be.null;
  });
});
