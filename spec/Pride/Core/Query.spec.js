const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Core.Query', function () {
  it('works', function () {
    expect(Pride.Core.Query).to.not.be.null;
  });
});
