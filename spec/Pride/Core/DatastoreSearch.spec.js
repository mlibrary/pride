const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Core.DatastoreSearch', function () {
  it('works', function () {
    expect(Pride.Core.DatastoreSearch).to.not.be.null;
  });
});
