const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Core.FacetSearch', function () {
  it('works', function () {
    expect(Pride.Core.FacetSearch).to.not.be.null;
  });
});
