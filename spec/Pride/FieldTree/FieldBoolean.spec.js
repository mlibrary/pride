const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.FieldTree.FieldBoolean', function () {
  it('works', function () {
    expect(Pride.FieldTree.FieldBoolean).to.not.be.null;
  });
});
