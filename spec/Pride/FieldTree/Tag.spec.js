const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.FieldTree.Tag', function () {
  it('works', function () {
    expect(Pride.FieldTree.Tag).to.not.be.null;
  });
});
