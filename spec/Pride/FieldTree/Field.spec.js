const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.FieldTree.Field', function () {
  it('works', function () {
    expect(Pride.FieldTree.Field).to.not.be.null;
  });
});
