const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Util.MultiSearch', function () {
  it('works', function () {
    expect(Pride.Util.MultiSearch).to.not.be.null;
  });
});
