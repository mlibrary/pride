const { expect } = require('chai');
const Pride = require('../../../pride').Pride;

describe('Pride.Util.SearchSwitcher', function () {
  it('works', function () {
    expect(Pride.Util.SearchSwitcher).to.not.be.null;
  });
});
