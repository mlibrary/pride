const { expect } = require('chai');
const Pride = require('../../pride').Pride;

describe('Pride.Settings', function () {
  it('works', function () {
    expect(Pride.Settings).to.not.be.null;
  });
});
