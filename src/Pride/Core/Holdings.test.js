import { expect } from 'chai';
import Holdings from './Holdings';

describe.only('Holdings()', () => {
  it('does a thing', () => {
    console.log(Holdings);
    expect(Holdings).to.not.be.null;
  });
});
