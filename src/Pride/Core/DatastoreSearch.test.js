import { expect } from 'chai';
import DatastoreSearch from './DatastoreSearch';

describe.only('DatastoreSearch()', () => {
  it('does a thing', () => {
    console.log(DatastoreSearch);
    expect(DatastoreSearch).to.not.be.null;
  });
});
