import { expect } from 'chai';
import MultiSearch from './MultiSearch';

describe.only('MultiSearch()', () => {
  it('does a thing', () => {
    console.log(MultiSearch);
    expect(MultiSearch).to.not.be.null;
  });
});
