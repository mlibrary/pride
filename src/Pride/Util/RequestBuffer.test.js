import { expect } from 'chai';
import RequestBuffer from './RequestBuffer';

describe.only('RequestBuffer()', () => {
  it('does a thing', () => {
    console.log(RequestBuffer);
    expect(RequestBuffer).to.not.be.null;
  });
});
