import { expect } from 'chai';
import SearchSwitcher from './SearchSwitcher';

describe.only('SearchSwitcher()', () => {
  it('does a thing', () => {
    console.log(SearchSwitcher);
    expect(SearchSwitcher).to.not.be.null;
  });
});
