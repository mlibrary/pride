import { expect } from 'chai';
import PreferenceEngine from './PreferenceEngine';

describe.only('PreferenceEngine()', () => {
  it('does a thing', () => {
    console.log(PreferenceEngine);
    expect(PreferenceEngine).to.not.be.null;
  });
});
